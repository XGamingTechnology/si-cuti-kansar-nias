# Template only. Copy to .env.edge.staging (ignored).
# Use a real staging hostname and certificate paths on the VPS.
STAGING_HOSTNAME=staging-hostname.example.invalid
STAGING_FRONTEND_NETWORK=si-cuti-staging-frontend
TLS_CERTIFICATE_PATH=/etc/si-cuti/tls/staging/fullchain.pem
TLS_PRIVATE_KEY_PATH=/etc/si-cuti/tls/staging/privkey.pem
