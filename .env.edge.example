# Template only. Copy to .env.edge (ignored). Domains and certificate paths are deployment inputs.
PRODUCTION_HOSTNAME=production-hostname.example.invalid
STAGING_HOSTNAME=staging-hostname.example.invalid
PRODUCTION_FRONTEND_NETWORK=si-cuti-prod-frontend
STAGING_FRONTEND_NETWORK=si-cuti-staging-frontend
TLS_CERTIFICATE_PATH=/etc/si-cuti/tls/fullchain.pem
TLS_PRIVATE_KEY_PATH=/etc/si-cuti/tls/privkey.pem
